import 'package:flutter/material.dart';

// Light theme colors
const mobileBackgroundColor = Color.fromRGBO(255, 255, 255, 1); // white
const webBackgroundColor = Color.fromRGBO(245, 245, 245, 1); // very light gray
const mobileSearchColor = Color.fromRGBO(230, 230, 230, 1); // light gray
const blueColor = Color.fromRGBO(0, 149, 246, 1); // bright blue
const primaryColor = Colors.white; // for backgrounds, etc.
const secondaryColor = Colors.grey; // text or icon color
const backgroundColor =  Colors.deepPurple;
const searchBarGray = Color(0xFFF1F1F1); 